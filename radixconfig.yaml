apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: pysand-web
spec:
  environments:
    - name: dev
      build:
        from: main
    - name: prod
      build:
        from: main
  components:
    - name: main
      src: "."
      publicPort: http
      ports:
       - name: http
         port: 5000
  dnsAppAlias:
    environment: dev
    component: main
